{
  "author": "@luis",
  "name": "lbs-ds",
  "description": "Monorepo strategy in design system",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "^npm@10.2.4",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.0.3"
  }
}